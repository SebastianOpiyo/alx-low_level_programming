#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 * @s: Pointer to the string
 *
 * Return: Always 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(*s);

	return (0);

}
