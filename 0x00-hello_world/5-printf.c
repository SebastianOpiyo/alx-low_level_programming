#include<stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 * Assignment: Use printf to print string on the screen.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
