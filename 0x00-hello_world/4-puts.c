#include<stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 * Use puts to print string on the screen.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
