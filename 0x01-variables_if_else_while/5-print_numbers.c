#include <stdlib.h>
#include <stdio.h>

/**
 * main - function that prints numbers 0-9.
 * Return: Always 0 (Success)
 * Print a new line too.
 */

int main(void)
{

	int x;

	for (x = 0; x < 10; x++)
	{
		printf("%d", x);
	}
	printf("\n");

	return (0);
}
