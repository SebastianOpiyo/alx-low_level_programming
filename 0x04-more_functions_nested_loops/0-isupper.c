#include "main.h"

/**
 * _isupper - Check if a character is Uppercase or lowercase
 * @c: parameter passed.
 * Return: 1 (Uppercase), otherwise 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'A')
		return (1);
	else
		return (0);
}
